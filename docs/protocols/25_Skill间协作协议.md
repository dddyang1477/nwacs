# 🤝 Skill 间协作协议
## Inter-Skill Collaboration Protocol (ISCP)

---

## 一、协议概述

本协议定义了系统中所有Skill之间的**通信规范、数据格式、协作规则**，确保38个Skill能够高效、有序、智能地协同工作。

---

## 二、通信协议

### 2.1 消息格式

所有Skill间的通信采用标准化消息格式：

```json
{
  "protocol_version": "1.0",
  "message_id": "uuid",
  "timestamp": "ISO8601",
  "from_skill": {
    "name": "skill_name",
    "level": 1|2|3,
    "instance_id": "uuid"
  },
  "to_skill": {
    "name": "skill_name",
    "level": 1|2|3,
    "instance_id": "uuid"
  },
  "message_type": "request|response|feedback|broadcast|memory_sync|error",
  "task_context": {
    "project_id": "project_uuid",
    "task_id": "task_uuid",
    "phase": "worldbuilding|character|plot|scene|dialogue|combat|writing|review",
    "priority": 1|2|3|4|5,
    "deadline": "ISO8601|null"
  },
  "payload": {
    "content": "具体内容",
    "format": "markdown|json|text",
    "attachments": [],
    "metadata": {}
  },
  "dependencies": {
    "requires": ["task_id_1", "task_id_2"],
    "blocks": ["task_id_3"]
  },
  "memory_reference": {
    "global_keys": [],
    "skill_keys": [],
    "type_keys": []
  }
}
```

### 2.2 消息类型

| 类型 | 用途 | 发送方 | 接收方 |
|------|------|--------|--------|
| `request` | 请求执行任务 | 任何Skill | 目标Skill |
| `response` | 返回执行结果 | 被请求Skill | 请求方 |
| `feedback` | 提供修改意见 | 监督模块 | 创作模块 |
| `broadcast` | 全局通知 | 一级Skill | 所有Skill |
| `memory_sync` | 同步记忆数据 | 任何Skill | 记忆中枢 |
| `error` | 报告错误/异常 | 任何Skill | 一级Skill |

---

## 三、协作规则

### 3.1 层级规则

```
┌─────────────────────────────────────────┐
│  一级Skill（小说总调度官）                │
│  → 可调用所有Skill                       │
│  → 可广播全局指令                        │
│  → 可终止任何任务                        │
└─────────────────────────────────────────┘
                    │
    ┌───────────────┼───────────────┐
    ▼               ▼               ▼
┌─────────┐   ┌─────────┐   ┌─────────┐
│二级创作  │   │二级监督  │   │二级学习  │
│→可调用  │   │→可反馈  │   │→可学习  │
│三级Skill│   │创作模块  │   │规则模块  │
│→可向同级│   │→可向一级│   │→可向一级│
│请求协作  │   │报告质量 │   │报告结果 │
└─────────┘   └─────────┘   └─────────┘
        │               │
        └───────┬───────┘
                ▼
        ┌─────────────────────┐
        │  三级Skill           │
        │  → 类型专家          │
        │  → 功能专家          │
        │  → 只能被动响应       │
        │  → 不可主动调用       │
        └─────────────────────┘
```

**规则明细**：

| 规则 | 说明 |
|------|------|
| R1 | 一级Skill可调用任何其他Skill |
| R2 | 二级Skill可调用同级Skill和三级Skill |
| R3 | 三级Skill只能被动响应，不能主动调用 |
| R4 | 监督模块可反馈给创作模块，但不能直接修改 |
| R5 | 所有Skill必须响应一级Skill的广播 |
| R6 | Skill可拒绝任务，但必须说明理由 |
| R7 | 二级Skill可根据需要拆分功能到三级Skill |
| R8 | 三级Skill专注于单一功能领域，提供专业支持 |

### 3.2 并行与串行

**并行执行**（无依赖关系）：
```
世界观构造 ──┐
              ├──→ 剧情构造 ──→ 场景/对话/战斗
角色塑造 ────┘
```

**串行执行**（有依赖关系）：
```
世界观构造 → 角色塑造 → 剧情构造 → 场景构造
```

**混合执行**（实际项目）：
```
                    ┌→ 场景构造 ─┐
世界观 → 角色 ──→ 剧情 ──┼→ 对话设计 ─┼→ 写作
                    └→ 战斗设计 ─┘
                              ↓
                        去AI痕迹 → 质量审计
```

### 3.3 冲突解决

当多个Skill输出冲突时：

1. **数据冲突**（如世界观与剧情矛盾）
   - 优先级：世界观 > 剧情 > 角色 > 场景
   - 由一级Skill裁定

2. **风格冲突**（如不同Skill文风不统一）
   - 由写作技巧大师统一
   - 以风格指南为准

3. **质量冲突**（如监督模块与创作模块分歧）
   - 监督模块提出具体问题
   - 创作模块可选择修改或申诉
   - 一级Skill最终裁定

---

## 四、上下文传递

### 4.1 上下文内容

每个Skill接收的上下文必须包含：

```json
{
  "project_context": {
    "name": "项目名称",
    "genre": "小说类型",
    "style": "风格要求",
    "scale": "规模"
  },
  "upstream_outputs": {
    "worldbuilding": "世界观摘要",
    "characters": "角色摘要",
    "plot": "剧情摘要"
  },
  "current_phase": "当前阶段",
  "user_preferences": {
    "style": "文风偏好",
    "taboos": "禁忌内容",
    "favorites": "偏好元素"
  },
  "memory_snapshot": {
    "global": "全局记忆摘要",
    "skill_specific": "该Skill相关记忆"
  }
}
```

### 4.2 上下文精简原则

- 只传递**必要信息**，避免信息过载
- 使用**摘要**而非全文
- 关键细节**标注引用**，需要时查阅原文
- 上下文长度控制在 **2000字以内**

---

## 五、数据交换标准

### 5.1 输出物规范

每个Skill的输出必须包含：

```markdown
# [Skill名称]输出：[任务名称]

## 元数据
- 任务ID：
- 版本：
- 依赖：
- 状态：完成/部分/阻塞

## 正文
[具体内容]

## 摘要
[200字以内的核心内容摘要]

## 传递项
[需要传递给下游Skill的关键信息]

## 待确认
[需要上游或用户确认的问题]
```

### 5.2 版本控制

- 每次修改递增版本号
- 保留修改历史
- 标注修改原因

---

## 六、错误处理

### 6.1 错误类型

| 代码 | 类型 | 说明 | 处理 |
|------|------|------|------|
| E001 | 依赖缺失 | 上游输出未完成 | 等待或跳过 |
| E002 | 上下文不足 | 信息不足以执行任务 | 请求补充 |
| E003 | 能力超限 | 任务超出Skill能力 | 转交或降级 |
| E004 | 输出冲突 | 与已有输出矛盾 | 上报裁定 |
| E005 | 质量不足 | 输出达不到标准 | 请求重做 |
| E006 | 记忆错误 | 记忆数据不一致 | 同步修复 |

### 6.2 错误上报流程

```
Skill发现错误
    ↓
尝试自我修复（3次）
    ↓
成功 → 继续
失败 → 上报一级Skill
    ↓
一级Skill裁定
    ↓
修复 / 绕过 / 终止
```

---

## 七、性能优化

### 7.1 超时机制

| Skill类型 | 超时时间 | 处理 |
|-----------|----------|------|
| 创作模块 | 5分钟 | 返回部分结果 |
| 监督模块 | 3分钟 | 简化审查 |
| 类型专家 | 2分钟 | 返回基础模板 |
| 一级Skill | 10分钟 | 强制汇总 |

### 7.2 缓存策略

- 相同输入直接返回缓存结果
- 类型知识库定期更新缓存
- 用户偏好长期缓存

---

## 八、安全与隐私

### 8.1 数据安全

- 所有Skill输出加密存储
- 用户敏感信息脱敏处理
- 定期清理过期记忆

### 8.2 权限控制

| 操作 | 一级Skill | 二级Skill | 三级Skill |
|------|-----------|-----------|-----------|
| 读取全局记忆 | ✅ | 部分 | ❌ |
| 修改全局记忆 | ✅ | ❌ | ❌ |
| 读取Skill记忆 | ✅ | 自己 | ❌ |
| 修改Skill记忆 | ✅ | 自己 | 自己 |
| 调用其他Skill | ✅ | 部分 | ❌ |
| 终止任务 | ✅ | ❌ | ❌ |

---

*协议版本：v1.0 | 生效日期：2026-04-27*
