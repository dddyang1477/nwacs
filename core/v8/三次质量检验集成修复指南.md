# NWACS FINAL - 三次质量检验集成修复指南

## 🎯 修复目标

将`three_time_quality_check.py`集成到`NWACS_FINAL.py`的主程序中，使得快速生成功能（选项18）能够使用完整的三次质量检验流程。

---

## 📝 需要修改的文件

**文件**: `NWACS_FINAL.py`  
**位置**: 第587-596行  
**方法**: `save_novel_to_folder`

---

## 🔄 需要替换的代码

### ❌ 旧代码（第587-596行）

```python
        # 3. 集成质量检测功能
        try:
            from quality_check_and_save_v2 import QualityChecker
            checker = QualityChecker(processed_opening, 1)
            passed, report = checker.run_all_checks()
            
            if not passed:
                print("⚠️ 质量检测提示：建议后续人工完善内容")
        except Exception as e:
            print(f"⚠️ 质量检测模块加载跳过: {e}")
```

### ✅ 新代码（替换为）

```python
        # 3. 集成质量检测功能（使用三次检验流程）
        print("\n" + "="*60)
        print("🔍 开始三次质量检验流程...")
        print("   最多检验3次，不合格将重新处理")
        print("="*60)
        
        try:
            # 导入三次检验模块
            from three_time_quality_check import call_three_time_quality_check
            
            # 使用完整的三次检验流程
            processed_opening, quality_passed, quality_report = call_three_time_quality_check(
                processed_opening,
                chapter_num=1,
                novel_title=novel_name
            )
            
            if quality_passed:
                print("✅ 三次检验全部通过！")
            else:
                print("⚠️ 三次检验未完全通过，已生成并保存，建议人工审核")
                
        except Exception as e:
            print(f"⚠️ 三次检验流程执行: {e}")
            print("   回退到基础质量检测...")
            try:
                from quality_check_and_save_v2 import QualityChecker
                checker = QualityChecker(processed_opening, 1)
                passed, report = checker.run_all_checks()
                
                if not passed:
                    print("⚠️ 质量检测提示：建议后续人工完善内容")
            except Exception as e2:
                print(f"⚠️ 质量检测模块加载跳过: {e2}")
```

---

## 📋 修复后的完整流程

修复后，`save_novel_to_folder`方法的完整流程将是：

```python
def save_novel_to_folder(self, novel_name, template, genre_name):
    """保存小说到文件夹"""
    
    # 1. 创建小说文件夹
    # ...
    
    # 2. 获取开局内容
    original_opening = template['example_opening']
    
    # 3. AI去痕模块处理（降低AI特征）
    processed_opening = original_opening
    try:
        from ai_detector_and_rewriter import AIDetectorAndRewriter
        detector = AIDetectorAndRewriter()
        processed_opening = detector.check_and_rewrite(original_opening)
    except:
        processed_opening = original_opening
    
    # 4. 三次质量检验流程 ⭐（新增！）
    try:
        from three_time_quality_check import call_three_time_quality_check
        processed_opening, quality_passed, quality_report = call_three_time_quality_check(
            processed_opening,
            chapter_num=1,
            novel_title=novel_name
        )
    except:
        # 回退到单次检测
        pass
    
    # 5. 保存文件
    # ...
```

---

## 🔧 修复步骤

### 步骤1：确认文件存在
```bash
cd core/v8
ls -la three_time_quality_check.py
```

### 步骤2：备份原文件
```bash
cp NWACS_FINAL.py NWACS_FINAL.py.backup
```

### 步骤3：应用修复
使用文本编辑器（如VS Code、Notepad++等）：
1. 打开 `NWACS_FINAL.py`
2. 找到第587-596行
3. 删除旧代码
4. 粘贴新代码
5. 保存文件

### 步骤4：验证修复
```bash
grep -n "three_time_quality_check" NWACS_FINAL.py
```
应该显示调用了 `from three_time_quality_check import`

---

## 📊 修复后的效果

### 修复前
- ❌ 单次质量检测
- ❌ 检测失败直接保存
- ❌ 无人工干预提示

### 修复后
- ✅ 三次质量检验流程
- ✅ 每次失败自动去痕优化
- ✅ 第三失败标记人工处理
- ✅ 完整检测报告

---

## 🎯 三次检验流程

```
第1次检验
    ↓
检测（字数/结构/可读性/结尾）
    ↓
通过？ → ✅ 保存，去痕版内容更新
    ↓ 否
第2次检验
    ↓
额外去痕优化
    ↓
检测
    ↓
通过？ → ✅ 保存，去痕版内容更新
    ↓ 否
第3次检验
    ↓
最终去痕优化
    ↓
检测
    ↓
通过？ → ✅ 保存
    ↓ 否
⚠️ 标记人工处理
```

---

## ⚠️ 注意事项

1. **必须先确保 `three_time_quality_check.py` 文件存在**
2. **备份原文件后再修改**
3. **保持代码缩进一致（4空格）**
4. **保存时使用UTF-8编码**

---

## ✅ 验证方法

修复后，运行主程序并选择选项18（快速生成开局），应该看到：

```
============================================================
🔍 开始AI检测与去痕...
============================================================

============================================================
🔍 三次质量检验流程启动（第1-3次）
============================================================

============================================================
📋 第1次检验
============================================================

检测结果:
   - word_count: 850
   - paragraphs: 12
   - check_readability: True
   - check_ending: True

✅ 第1次检验通过！
```

---

## 📞 常见问题

### Q: 修复脚本运行失败？
**A**: 由于编码问题，建议手动应用修复，详见上述步骤。

### Q: 修改后程序报错？
**A**: 检查代码缩进是否正确，确保使用4空格缩进。

### Q: 三次检验还是没生效？
**A**: 确认 `three_time_quality_check.py` 文件存在于同一目录下。

---

**修复指南创建完成！按照上述步骤即可完成三次质量检验的集成。**
